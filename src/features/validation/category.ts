const minLength = 2;
const maxLength = 30;

export const categoryNameRules = {
  minLength: minLength,
  maxLength: maxLength,
  minLengthMsg: `Min ${minLength} characters`,
  maxLengthMsg: `Max ${maxLength} characters`,
};
